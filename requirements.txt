fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# HuggingFace stack
# IndicTransToolkit v1.1.1.post1 README explicitly requires transformers>=4.51
# (older versions crash IndicTrans2's custom tokenizer with
#  "got multiple values for keyword argument 'src_vocab_file'").
transformers>=4.51,<4.55
accelerate>=1.5.2
safetensors>=0.4.3
sentencepiece>=0.2.0
sacremoses>=0.1.1

# Required by transformers' slow tokenizers (sentencepiece path) — missing this
# causes ImportError on model load:
#   "requires the protobuf library but it was not found in your environment"
protobuf>=3.20,<5

# IndicTrans2 pre/post-processing toolkit
git+https://github.com/VarunGumma/IndicTransToolkit.git@v1.1.1.post1

# PyTorch — the pytorch/pytorch base image already provides it. Don't reinstall
# (would risk overriding the CUDA-matched wheel).
